<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <a href="/logout">Logout</a>
    <% if (quote) { %>
       <h1><%= quote %></h1> 
    <% } %>
    <% if (linkpayment) {%>
    <a href="/payment">LINK PAYMENT</a>
    <h1></h1>
    <img src="<%= pict %>" alt="">
    <% } %>
</body>
</html>